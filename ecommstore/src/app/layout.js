import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const metadata = {
  title: "Ecom Store - Your One-Stop Online Shopping Destination",
  description: "Ecom Store offers a wide range of products at unbeatable prices. Shop now for the latest trends, exclusive deals, and fast shipping on all your favorite items.",
};

export { metadata };

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}